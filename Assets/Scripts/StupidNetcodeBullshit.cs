using System.Collections;
using System.Collections.Generic;
using Unity.Netcode;
using UnityEngine;
using static NetworkAdaptor;

// This class is in dedication to Larah Armstrong and the team over at Netcode for GameObjects, whose stupid-ass documentation should be held as legend status in the realm of crappy bullshit. Quite possibly the worst garbage I have ever read :)
public static class StupidNetcodeBullshit
{
    /*
    public static void ReadValueSafe(this FastBufferReader reader, out ObjectProtocolBuffer protobuf)
    {
        reader.ReadValueSafe(out string val);
        protobuf = new ObjectProtocolBuffer(val);
    }

    public static void WriteValueSafe(this FastBufferWriter writer, in ObjectProtocolBuffer protobuf)
    {
        writer.WriteValueSafe(protobuf.ToString());
    }
    */
}
